% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{sd_txt2img_highres}
\alias{sd_txt2img_highres}
\title{High-resolution image generation via patch-based pipeline}
\usage{
sd_txt2img_highres(
  ctx,
  prompt,
  negative_prompt = "",
  width = 2048L,
  height = 2048L,
  tile_size = NULL,
  overlap = 0.125,
  img2img_strength = NULL,
  sample_method = SAMPLE_METHOD$EULER,
  sample_steps = 20L,
  cfg_scale = 7,
  seed = 42L,
  scheduler = SCHEDULER$DISCRETE,
  clip_skip = -1L,
  eta = 0,
  vae_mode = "auto",
  vae_auto_threshold = 1048576L,
  vae_tile_size = 64L,
  vae_tile_overlap = 0.25
)
}
\arguments{
\item{ctx}{SD context created by \code{\link{sd_ctx}}}

\item{prompt}{Text prompt}

\item{negative_prompt}{Negative prompt (default "")}

\item{width}{Target image width in pixels}

\item{height}{Target image height in pixels}

\item{tile_size}{Patch size in pixels. \code{NULL} = auto-detect from
\code{model_type} attribute on \code{ctx} (512 for SD1/SD2, 1024 for
SDXL/Flux/SD3). Must be divisible by 8.}

\item{overlap}{Overlap between patches as fraction of \code{tile_size},
0.0-0.5 (default 0.125).}

\item{img2img_strength}{If not \code{NULL}, run a final \code{img2img} pass
over the stitched image at this denoising strength (e.g. 0.3) to
harmonize seams. Requires \code{vae_decode_only = FALSE} in the context.
Default \code{NULL} (disabled).}

\item{sample_method}{Sampling method (see \code{SAMPLE_METHOD})}

\item{sample_steps}{Number of sampling steps (default 20)}

\item{cfg_scale}{Classifier-free guidance scale (default 7.0)}

\item{seed}{Base random seed. Each patch gets \code{seed + patch_index}.
Use -1 for random.}

\item{scheduler}{Scheduler type (see \code{SCHEDULER})}

\item{clip_skip}{Number of CLIP layers to skip (-1 = auto)}

\item{eta}{Eta parameter for DDIM-like samplers}

\item{vae_mode}{VAE tiling mode for the harmonization pass
(default \code{"auto"}, see \code{\link{sd_txt2img}}).}

\item{vae_auto_threshold}{Pixel area threshold for auto VAE tiling}

\item{vae_tile_size}{Tile size for VAE tiling (default 64)}

\item{vae_tile_overlap}{Overlap for VAE tiling (default 0.25)}
}
\value{
SD image (list with width, height, channel, data)
}
\description{
Generates a large image by independently rendering overlapping patches at
the model's native resolution, then stitching them with linear blending.
An optional \code{img2img} harmonization pass can smooth seams further.
}
\examples{
\dontrun{
ctx <- sd_ctx("sd15.safetensors", model_type = "sd1")
img <- sd_txt2img_highres(ctx, "a panoramic mountain landscape",
                          width = 2048, height = 1024)
sd_save_image(img, "panorama.png")
}
}
\keyword{internal}
