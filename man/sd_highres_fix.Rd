% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{sd_highres_fix}
\alias{sd_highres_fix}
\title{High-resolution image generation (Highres Fix)}
\usage{
sd_highres_fix(
  ctx,
  prompt,
  negative_prompt = "",
  width = 2048L,
  height = 2048L,
  sample_method = SAMPLE_METHOD$EULER,
  sample_steps = 20L,
  cfg_scale = 7,
  seed = 42L,
  scheduler = SCHEDULER$DISCRETE,
  clip_skip = -1L,
  eta = 0,
  hr_strength = 0.4,
  hr_steps = NULL,
  sample_tile_size = NULL,
  sample_tile_overlap = 0.25,
  upscaler = NULL,
  upscale_factor = 4L,
  vae_mode = "auto",
  vae_auto_threshold = 1048576L,
  vae_tile_size = 64L,
  vae_tile_overlap = 0.25
)
}
\arguments{
\item{ctx}{SD context created by \code{\link{sd_ctx}}}

\item{prompt}{Text prompt describing desired image}

\item{negative_prompt}{Negative prompt (default "")}

\item{width}{Target output width in pixels (default 2048)}

\item{height}{Target output height in pixels (default 2048)}

\item{sample_method}{Sampling method (see \code{SAMPLE_METHOD})}

\item{sample_steps}{Number of sampling steps (default 20)}

\item{cfg_scale}{Classifier-free guidance scale (default 7.0)}

\item{seed}{Random seed (-1 for random)}

\item{scheduler}{Scheduler type (see \code{SCHEDULER})}

\item{clip_skip}{Number of CLIP layers to skip (-1 = auto)}

\item{eta}{Eta parameter for DDIM-like samplers}

\item{hr_strength}{Denoising strength for the refinement pass (0.0-1.0,
default 0.4). Lower = more faithful to base, higher = more detail/change.}

\item{hr_steps}{Sample steps for refinement pass (default same as sample_steps)}

\item{sample_tile_size}{Tile size in latent pixels for refinement (default auto)}

\item{sample_tile_overlap}{Tile overlap fraction (default 0.25)}

\item{upscaler}{Path to ESRGAN model for upscaling. If NULL, uses bilinear.}

\item{upscale_factor}{ESRGAN upscale factor (default 4, only used with upscaler)}

\item{vae_mode}{VAE processing mode: \code{"normal"} (no tiling),
\code{"tiled"} (always tile), or \code{"auto"} (tile when
\code{width * height > vae_auto_threshold}). Default \code{"auto"}.}

\item{vae_auto_threshold}{Pixel area threshold for \code{vae_mode = "auto"}.
Tiling activates when \code{width * height} exceeds this value.
Default \code{1048576L} (1024x1024 pixels). Adjust for your VRAM budget.}

\item{vae_tile_size}{Tile size in latent pixels for tiled VAE (default 64).
Ignored when \code{vae_tile_rel_x}/\code{vae_tile_rel_y} are set.}

\item{vae_tile_overlap}{Overlap ratio between tiles, 0.0-0.5 (default 0.25)}
}
\value{
SD image (single image, not list)
}
\description{
Two-pass generation: first creates a base image at native model resolution,
then upscales and refines with tiled img2img to produce a high-resolution
result with coherent global composition.
}
\keyword{internal}
