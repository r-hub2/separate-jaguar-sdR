% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{sd_generate_multi_gpu}
\alias{sd_generate_multi_gpu}
\title{Parallel generation across multiple GPUs}
\usage{
sd_generate_multi_gpu(
  model_path = NULL,
  prompts,
  negative_prompt = "",
  devices = NULL,
  seeds = NULL,
  width = 512L,
  height = 512L,
  model_type = "sd1",
  vram_gb = NULL,
  vae_decode_only = TRUE,
  progress = TRUE,
  diffusion_model_path = NULL,
  vae_path = NULL,
  clip_l_path = NULL,
  t5xxl_path = NULL,
  ...
)
}
\arguments{
\item{model_path}{Path to the model file (single-file models like SD 1.x/2.x/SDXL)}

\item{prompts}{Character vector of prompts (one image per prompt)}

\item{negative_prompt}{Negative prompt applied to all images (default "")}

\item{devices}{Integer vector of Vulkan device indices (0-based). Default
\code{NULL} auto-detects all available devices.}

\item{seeds}{Integer vector of seeds, same length as \code{prompts}. Default
\code{NULL} generates random seeds.}

\item{width}{Image width (default 512)}

\item{height}{Image height (default 512)}

\item{model_type}{Model type (default "sd1")}

\item{vram_gb}{VRAM per GPU for auto-routing (default NULL)}

\item{vae_decode_only}{VAE decode only (default TRUE)}

\item{progress}{Print progress messages (default TRUE)}

\item{diffusion_model_path}{Path to diffusion model (Flux/multi-file models)}

\item{vae_path}{Path to VAE model}

\item{clip_l_path}{Path to CLIP-L model}

\item{t5xxl_path}{Path to T5-XXL model}

\item{...}{Additional arguments passed to \code{\link{sd_generate}}}
}
\value{
List of SD images, one per prompt, in original order.
}
\description{
Distributes prompts across available Vulkan GPUs, running one process per
GPU via \code{callr}. Each process creates its own \code{\link{sd_ctx}} and
calls \code{\link{sd_generate}}. Requires the \code{callr} package.
}
\note{
Release any existing SD context (\code{rm(ctx); gc()}) before calling
  this function. Holding a Vulkan context in the main process while
  subprocesses try to use the same GPU can produce corrupted (grey) images.
}
\examples{
\dontrun{
# Single-file model (SD 1.x/2.x/SDXL)
imgs <- sd_generate_multi_gpu(
  "model.safetensors",
  prompts = c("a cat", "a dog", "a bird", "a fish"),
  devices = 0:1
)

# Multi-file model (Flux)
imgs <- sd_generate_multi_gpu(
  diffusion_model_path = "flux1-dev-Q4_K_S.gguf",
  vae_path = "ae.safetensors",
  clip_l_path = "clip_l.safetensors",
  t5xxl_path = "t5-v1_1-xxl-encoder-Q5_K_M.gguf",
  prompts = c("a cat", "a dog"),
  model_type = "flux", devices = 0:1
)
}
}
