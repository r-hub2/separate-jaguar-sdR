% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{sd_ctx}
\alias{sd_ctx}
\title{Create a Stable Diffusion context}
\usage{
sd_ctx(
  model_path = NULL,
  vae_path = NULL,
  taesd_path = NULL,
  clip_l_path = NULL,
  clip_g_path = NULL,
  t5xxl_path = NULL,
  diffusion_model_path = NULL,
  control_net_path = NULL,
  n_threads = 0L,
  wtype = SD_TYPE$COUNT,
  vae_decode_only = TRUE,
  free_params_immediately = FALSE,
  keep_clip_on_cpu = FALSE,
  keep_vae_on_cpu = FALSE,
  diffusion_flash_attn = FALSE,
  rng_type = RNG_TYPE$CUDA,
  prediction = NULL,
  lora_apply_mode = LORA_APPLY_MODE$AUTO,
  flow_shift = 0,
  model_type = "sd1",
  vram_gb = NULL,
  verbose = FALSE
)
}
\arguments{
\item{model_path}{Path to the model file (safetensors, gguf, or checkpoint)}

\item{vae_path}{Optional path to a separate VAE model}

\item{taesd_path}{Optional path to TAESD model for preview}

\item{clip_l_path}{Optional path to CLIP-L model}

\item{clip_g_path}{Optional path to CLIP-G model}

\item{t5xxl_path}{Optional path to T5-XXL model}

\item{diffusion_model_path}{Optional path to separate diffusion model}

\item{control_net_path}{Optional path to ControlNet model}

\item{n_threads}{Number of CPU threads (0 = auto-detect)}

\item{wtype}{Weight type for quantization (see \code{SD_TYPE})}

\item{vae_decode_only}{If TRUE, only load VAE decoder (saves memory)}

\item{free_params_immediately}{Free model params after first computation.
If TRUE, the context can only be used for a single generation — subsequent
calls will crash. Set to TRUE only when you need to save memory and will
not reuse the context. Default is FALSE.}

\item{keep_clip_on_cpu}{Keep CLIP model on CPU even when using GPU}

\item{keep_vae_on_cpu}{Keep VAE on CPU even when using GPU}

\item{diffusion_flash_attn}{Enable flash attention for diffusion model}

\item{rng_type}{RNG type (see \code{RNG_TYPE})}

\item{prediction}{Prediction type override (see \code{PREDICTION}), NULL = auto}

\item{lora_apply_mode}{LoRA application mode (see \code{LORA_APPLY_MODE})}

\item{flow_shift}{Flow shift value for Flux models}

\item{model_type}{Model architecture hint: \code{"sd1"}, \code{"sd2"},
\code{"sdxl"}, \code{"flux"}, or \code{"sd3"}. Used by
\code{\link{sd_generate}} to determine native resolution and tile sizes.
Default \code{"sd1"}.}

\item{vram_gb}{Override available VRAM in GB. When set, disables auto-detection
and uses this value for strategy routing. Default \code{NULL} (auto-detect
from Vulkan device).}

\item{verbose}{If \code{TRUE}, print model loading progress and sampling
steps. Default \code{FALSE}.}
}
\value{
An external pointer to the SD context (class "sd_ctx") with
  attributes \code{model_type}, \code{vae_decode_only}, \code{vram_gb},
  \code{vram_total_gb}, and \code{vram_device}.
}
\description{
Loads a model and creates a context for image generation.
}
\examples{
\dontrun{
ctx <- sd_ctx("model.safetensors")
imgs <- sd_txt2img(ctx, "a cat sitting on a chair")
sd_save_image(imgs[[1]], "cat.png")
}
}
