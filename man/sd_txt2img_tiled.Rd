% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{sd_txt2img_tiled}
\alias{sd_txt2img_tiled}
\title{Tiled diffusion sampling (MultiDiffusion)}
\usage{
sd_txt2img_tiled(
  ctx,
  prompt,
  negative_prompt = "",
  width = 2048L,
  height = 2048L,
  sample_tile_size = NULL,
  sample_tile_overlap = 0.25,
  sample_method = SAMPLE_METHOD$EULER,
  sample_steps = 20L,
  cfg_scale = 7,
  seed = 42L,
  batch_count = 1L,
  scheduler = SCHEDULER$DISCRETE,
  clip_skip = -1L,
  eta = 0,
  vae_mode = "auto",
  vae_auto_threshold = 1048576L,
  vae_tile_size = 64L,
  vae_tile_overlap = 0.25,
  vae_tile_rel_x = NULL,
  vae_tile_rel_y = NULL
)
}
\arguments{
\item{ctx}{SD context created by \code{\link{sd_ctx}}}

\item{prompt}{Text prompt describing desired image}

\item{negative_prompt}{Negative prompt (default "")}

\item{width}{Target image width in pixels (can exceed model native resolution)}

\item{height}{Target image height in pixels}

\item{sample_tile_size}{Tile size in latent pixels (default \code{NULL} =
auto from \code{model_type}: 64 for SD1/SD2, 128 for SDXL/Flux/SD3).
One latent pixel = \code{vae_scale_factor} image pixels (typically 8).}

\item{sample_tile_overlap}{Overlap between tiles as fraction of tile size,
0.0-0.5 (default 0.25).}

\item{sample_method}{Sampling method (see \code{SAMPLE_METHOD})}

\item{sample_steps}{Number of sampling steps (default 20)}

\item{cfg_scale}{Classifier-free guidance scale (default 7.0)}

\item{seed}{Random seed (-1 for random)}

\item{batch_count}{Number of images to generate (default 1)}

\item{scheduler}{Scheduler type (see \code{SCHEDULER})}

\item{clip_skip}{Number of CLIP layers to skip (-1 = auto)}

\item{eta}{Eta parameter for DDIM-like samplers}

\item{vae_mode}{VAE processing mode: \code{"normal"} (no tiling),
\code{"tiled"} (always tile), or \code{"auto"} (tile when
\code{width * height > vae_auto_threshold}). Default \code{"auto"}.}

\item{vae_auto_threshold}{Pixel area threshold for \code{vae_mode = "auto"}.
Tiling activates when \code{width * height} exceeds this value.
Default \code{1048576L} (1024x1024 pixels). Adjust for your VRAM budget.}

\item{vae_tile_size}{Tile size in latent pixels for tiled VAE (default 64).
Ignored when \code{vae_tile_rel_x}/\code{vae_tile_rel_y} are set.}

\item{vae_tile_overlap}{Overlap ratio between tiles, 0.0-0.5 (default 0.25)}

\item{vae_tile_rel_x}{Relative tile width as fraction of latent width (0-1)
or number of tiles (>1). NULL = use \code{vae_tile_size}. Takes priority
over \code{vae_tile_size}.}

\item{vae_tile_rel_y}{Relative tile height as fraction of latent height (0-1)
or number of tiles (>1). NULL = use \code{vae_tile_size}. Takes priority
over \code{vae_tile_size}.}
}
\value{
List of SD images
}
\description{
Generates images at any resolution using tiled sampling: at each denoising
step the latent is split into overlapping tiles, each tile is denoised
independently by the UNet, and results are merged with Gaussian weighting.
VRAM usage is bounded by tile size, not output resolution.
}
\details{
Requires tiled VAE (enabled automatically via \code{vae_mode = "auto"}).
}
\examples{
\dontrun{
ctx <- sd_ctx("sd15.safetensors", model_type = "sd1")
imgs <- sd_txt2img_tiled(ctx, "a vast mountain landscape",
                         width = 2048, height = 1024)
sd_save_image(imgs[[1]], "landscape.png")
}
}
\keyword{internal}
